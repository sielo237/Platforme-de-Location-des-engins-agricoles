export const getConstencyRatio = (matrix) => {
    
}

//avoir la somme pour le pair wise comparison matrix
const getSum = (matrix) => {
    const sum = [0,0,0];
    for(let i =0; i<matrix[0].length ; i++){
        for(let j = 0; j<matrix[0].length ; j++){
            
        }
    }
}